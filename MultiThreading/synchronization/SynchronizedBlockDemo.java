package MultiThreading.synchronization;
//if very few lines of code requires synchronization then it is not recommended to declare entire method as synchronized.
//we have to enclose those few lines of code using synchronized block.
//it reduces waiting time and improves performance.

public class SynchronizedBlockDemo {
}
